#include <stdio.h>

int get_number(int idx)
{
	if (idx == 0)
		return 9;
	if (idx == 1)
		return 8;
	if (idx == 2)
		return 10;
	if (idx == 3)
		return 24;
	if (idx == 4)
		return 75;
	if (idx == 5)
		return 9; 
	return 0;
}
